This is an OntoWeaver adapter for mapping Open Targets' Drug-Target Interactions
into the Semantic Knowledge Graph of your choice, using a causal schema.

By defaults, it targets OncodashKB.

Run `./prepare.sh` to download the data.
Run `./make.sh` to create the SKG.

